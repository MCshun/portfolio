doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title パスワードを忘れた方
    link(rel="stylesheet", href="/stylesheets/forgot-password.css")
  body
    main
      .container
        h1 パスワードリセット
        p メールアドレスを入力してください。修正用のリンクをお送りします。
        if errorMessage
          p.error-message= errorMessage 
          // 赤文字でエラーを表示
        form(action="/forgot-password" method="POST")
          label(for="email") メールアドレス
          input#email(type="email" name="email" placeholder="example@example.com" required)
          button(type="submit") リセットリンクを送信
